# Future Work

## ELK Layout Enhancements
- Revisit `ELK` if the governance and country views need stronger orthogonal routing, compound hierarchy handling, or more editorial layout control than Cytoscape's built-in layouts provide.
- The likely integration path is to generate view-specific coordinates with `ELK` and render them through Cytoscape's preset positioning rather than replacing Cytoscape as the interaction layer.

